<div class="container pt-5">

<h1>Mes listes de films</h1>

<% @lists.each do |list| %>
<%= link_to list.name, list_path(list.id) %>
<%= link_to "Ajouter un film", new_list_bookmark_path(list.id) %>


<br>
<% end %>

<%= link_to "Créer une nouvelle liste" , new_list_path %>

</div>
